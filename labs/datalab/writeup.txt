1.
a^b = a&~b | ~a&b = ~(~(a&~b)&(~a&b))
1001
1100
0101
3.
``a == b  <=>  !(a ^ b)``
x+1=~x
01111111+1=10000000
11111111+1=00000000
4.
a = a + (a << 8) + (a << 16) + (a << 24);
!(a ^ (x & a))
10101010
6.
0000-0111   >>3 == 0
1000,1001   >>1 == 100
7.
```
a & 00 == 0
a & FF == a 
```
0 -> 1 -> 0
x -> 0 -> -1
8.
y-x >=0 1
     <0 0
y>0,x<0;

9.
0 1111 0 
x      
10.
int conditional(int x, int y, int z) {
  x = !x + ~0;
  return (x & y) + (~x & z);
}
